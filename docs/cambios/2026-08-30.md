# 30 de agosto de 2026

## Control Red · contrato Nexus `8084`

- Confirmado desde `ratienza/control-red` que el demo read-only debe ser accesible únicamente por la LAN en `192.168.18.220:8084`.
- Sustituido `network_mode: host` por el bind explícito `192.168.18.220:8084:8084`, conservando la lectura de la tabla ARP del host mediante un montaje read-only.
- Fusionada `control-red#3` con CI verde; Nexus quedó en `control-red@d3a05ac` y el contenedor se recreó sin modificar los servicios `80`, `8081`, `8082` ni `8083`.
- Verificados HTTP `200` en `/` y `/health` desde la LAN, checkout limpio y ausencia de reinicios del contenedor.
