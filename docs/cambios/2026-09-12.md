# 12 de septiembre de 2026

## RL-CE-DOC-001 · Cierre documental de Cartera Estratégica v1.0 LAN

- Registrada Cartera Estratégica v1.0.0 como aplicación Streamlit privada desplegada en Nexus mediante Docker Compose y accesible solo por LAN en `192.168.18.220:8085`.
- Actualizadas la ficha propia, App Launch, el catálogo de aplicaciones, inventarios, puertos, arquitectura, Change Log y pendientes.
- Documentada la migración íntegra de SQLite mediante backup consistente, staging, validación y promoción atómica, sin exponer datos financieros ni rutas privadas.
- Registrado CE-SEC-001 según su estado real en `main`: PIN independiente desplegado y Google OIDC terminado en código, pero desactivado hasta disponer de URL HTTPS, callback y secretos externos.
- Conservados fuera de Git datos, bases, secretos, backups y exportaciones; la demo anterior permanece archivada y recuperable.
- Confirmado que App Launch usa el puerto canónico `80`, que `8080` permanece libre y que los servicios `8081–8084` no se modificaron.
- Delimitado como único bloque pendiente de publicación externa el alta de Cartera en el Tunnel existente, la configuración externa de Google y su validación real, sin apertura de puertos del router.

No se modificaron Cartera, su SQLite, App Launch, Cloudflare, DNS, Access, Tunnel, router ni otros runtimes. Los HTML/PDF se regeneraron exclusivamente desde las fuentes Markdown mediante el pipeline oficial.
